# Knowledge Return — EXP006 Chaos Frog

## experiment
Abuse Actor Port + handoff with unknown tools, bad JSON, duplicate/conflict delivers.

## hypothesis
Failure semantics matter more than happy-path polish for MOMO-relevant design.

## what_was_tried
See `evidence/exp006/FAILURE_TABLE.md`.

## worked
Idempotent identical re-deliver; conflict fail-closed; missing source loud fail.

## failed / surprise
Non-object `args` was silently coerced to `{}` — fixed to HTTP 400 after observation.

## reusable_pattern
Failure table: stimulus → observed → implication. Prefer fail-closed on identity conflicts.

## possible_momo_implication
Idempotency keys + conflict policy belong at transport ports, not only in business Actors.

## evidence
`evidence/exp006/`

## next_question
Who owns retry across Actor death — port, caller, or ledger?
